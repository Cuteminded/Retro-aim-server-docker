{
    "files.eol": "\n",
    "containers.commands.build": "${containerCommand} buildx build --platform linux/amd64,linux/arm64 --pull --push --rm -f \"${dockerfile}\" -t cuteminded/${tag} \"${context}\""
}
